package com.snapworks.pages;

public class CommomMethods {

	/*
	 * public void checkPageIsReady() { JavascriptExecutor javaScript =
	 * (JavascriptExecutor) getDriver(); try { for (int i = 0; i < 30; i++) {
	 * 
	 * Thread.sleep(1000); if ("complete".equalsIgnoreCase(javaScript.
	 * executeScript("return document.readyState").toString())) {
	 * Thread.sleep(3000); break; } } } catch (Exception ex) {
	 * Reporter.log("caught exception while loading" + ex.getMessage()); }
	 * 
	 * }
	 */
}
